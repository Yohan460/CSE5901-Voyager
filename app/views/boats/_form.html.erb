<%= form_with(model: boat, local: true) do |form| %>
  <% if boat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(boat.errors.count, "error") %> prohibited this boat from being saved:</h2>

      <ul>
      <% boat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Name %>
    <%= form.text_field :Name, id: :boat_Name %>
  </div>

  <div class="field">
    <%= form.label :NumberOfRooms %>
    <%= form.text_field :NumberOfRooms, id: :boat_NumberOfRooms %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
